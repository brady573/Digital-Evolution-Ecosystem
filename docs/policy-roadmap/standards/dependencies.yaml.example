schema: repo-ai/v1
rules:
  - id: dependencies.prefer-existing
    level: guidance
    scope: "**"
    instruction: >
      Prefer existing dependencies before introducing new ones.

  - id: dependencies.github-actions-pinned
    level: enforce
    scope: ".github/workflows/**"
    requirement: github_actions_must_be_pinned
