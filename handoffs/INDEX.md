# Handoff ledger

Every handoff in the queue, both directions. `pnpm test:handoffs` checks that
this file mentions every handoff file that exists and no handoff file that does
not. Add a row when you add a file.

| id | direction | status | title |
|---|---|---|---|
| _(none yet)_ | | | |

See `README.md` for the workflow and `TEMPLATE.md` for the format.
