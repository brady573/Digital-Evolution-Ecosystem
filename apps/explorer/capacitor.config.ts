import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.digitalevolution.explorer",
  appName: "Digital Evolution",
  webDir: "dist",
};

export default config;
